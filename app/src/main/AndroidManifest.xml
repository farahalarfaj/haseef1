<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.haseef4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Haseef4">
        <activity
            android:name=".restock"
            android:label="@string/title_activity_restock"
            android:theme="@style/Theme.Haseef4.NoActionBar"></activity>
        <activity
            android:name=".product"
            android:label="@string/title_activity_product"
            android:theme="@style/Theme.Haseef4.NoActionBar" />
        <activity android:name=".popUpWindow" />
        <activity android:name=".statistic" />
        <activity android:name=".StaffHome" />
        <activity android:name=".AdminHome" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>